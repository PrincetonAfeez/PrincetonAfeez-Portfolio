# Image Assets

Place the final headshot here as:

`headshot.jpg`, `headshot.png`, or `headshot.webp`

Preferred final name for implementation: `headshot.webp`.

The build can also include JPEG or PNG fallbacks if you provide them.
